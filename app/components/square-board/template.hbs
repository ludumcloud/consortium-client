<div class="square-board">
  <div class="square-grid">
    {{#each this.grid as |row|}}
      <div class="row">
        {{#each row as |tile|}}
          <SquareBoard::SquareTile @game={{@game}} @tile={{tile}} />
        {{/each}}
      </div>
    {{/each}}
  </div>
</div>
