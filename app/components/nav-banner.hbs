<nav class='nav-banner'>
  <span class='game-title'>
    CONSORTIUM
  </span>

  <div class='session-info'>
    {{#if @session.isAuthenticated}}
      Welcome, {{@session.user.username}}
      <span>|</span>
      <button type="button" {{on "click" @session.logout}}>Logout</button>
    {{else}}
      {{#link-to 'login'}}
        Login
      {{/link-to}}
      <span>
        |
      </span>
      {{#link-to 'register'}}
        Register
      {{/link-to}}
    {{/if}}
  </div>
</nav>
